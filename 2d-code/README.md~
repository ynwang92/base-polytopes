Code to compute toric bases in julia

cd ~/.julia/dev/ToricBases

using Pkg
Pkg.activate(".")  # Activate the local package
Pkg.test()         # Run all tests

git tag -a v1.0 -m "Version 1.0"
git push origin v1.0